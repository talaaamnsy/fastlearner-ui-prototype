import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { ChevronRight, RotateCcw } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Metric, MiniLine, Page, PageTitle, ProgressBar, Surface } from "@/components/app-ui";
import { progress } from "@/data/mock-data";
export const Route = createFileRoute("/progress")({ head: () => ({ meta: [{ title: "Progress — FastLearner" }, { name: "description", content: "Understand your accuracy, study activity, and weak areas." }, { property: "og:title", content: "Progress — FastLearner" }, { property: "og:description", content: "Understand your accuracy, study activity, and weak areas." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ProgressPage });
function ProgressPage() { const [range, setRange] = useState("Week"); return <Page><PageTitle title="Progress" subtitle="A clear view of where you're improving." /><div className="mb-6 flex w-fit gap-1 rounded-full bg-secondary p-1">{["Day", "Week", "Month", "All Time"].map((item) => <button key={item} onClick={() => setRange(item)} className={`rounded-full px-4 py-2 text-sm font-semibold transition-colors ${range === item ? "bg-card text-foreground shadow-soft" : "text-muted-foreground"}`}>{item}</button>)}</div><div className="grid grid-cols-2 gap-3 md:grid-cols-4"><Surface><Metric label="Accuracy" value="76%" /></Surface><Surface><Metric label="Questions" value="184" /></Surface><Surface><Metric label="Study Time" value="5h 12m" /></Surface><Surface><Metric label="Current Streak" value="7 days" /></Surface></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.4fr_0.8fr]"><Surface><h2 className="font-display text-xl font-bold">Accuracy</h2><p className="mt-1 text-sm text-muted-foreground">Your {range.toLowerCase()} trend</p><div className="mt-7"><MiniLine values={progress.weeklyAccuracy} /></div></Surface><Surface><h2 className="font-display text-xl font-bold">Performance by Exam</h2><div className="mt-6 space-y-5">{[["SKD",76],["TWK",64],["TIU",72],["TKP",81]].map(([name, value]) => <div key={String(name)}><div className="mb-2 flex justify-between text-sm"><span>{name}</span><strong>{value}%</strong></div><ProgressBar value={Number(value)} /></div>)}</div></Surface></div><div className="mt-5 grid gap-5 md:grid-cols-2"><Surface><h2 className="font-display text-xl font-bold">Weak Areas</h2><div className="mt-5 divide-y divide-border">{[["Numerik Berhitung","61%","tiu-4"],["Verbal Analogi","67%","tiu-1"]].map(([name,value,id]) => <Link key={id} to="/material/$materialId" params={{ materialId: id }} className="flex items-center justify-between py-4"><span className="font-medium">{name}</span><span className="flex items-center gap-2 font-semibold text-muted-foreground">{value}<ChevronRight className="h-4 w-4" /></span></Link>)}</div></Surface><Surface className="flex items-center justify-between gap-4"><div><div className="mb-4 flex h-10 w-10 items-center justify-center rounded-xl bg-secondary"><RotateCcw className="h-5 w-5" /></div><h2 className="font-display text-xl font-bold">Needs Review</h2><p className="mt-1 text-muted-foreground">12 questions</p></div><Button asChild><Link to="/question" search={{ source: "review", material: "tiu-4" }}>Review</Link></Button></Surface></div></Page>; }