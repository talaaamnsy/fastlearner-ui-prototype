import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, BookOpenCheck, Clock3 } from "lucide-react";
import { Page, PageTitle, Surface } from "@/components/app-ui";
import { exams } from "@/data/mock-data";

export const Route = createFileRoute("/learn")({
  head: () => ({ meta: [{ title: "Learn — FastLearner" }, { name: "description", content: "Choose an exam and build mastery topic by topic." }, { property: "og:title", content: "Learn — FastLearner" }, { property: "og:description", content: "Choose an exam and build mastery topic by topic." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: LearnPage,
});
function LearnPage() { return <Page><PageTitle title="Learn" subtitle="Choose what you want to study." /><div className="grid gap-5 md:grid-cols-2">{exams.map((exam) => exam.available ? <Link key={exam.id} to="/learn/skd" className="group"><Surface className="flex min-h-60 flex-col justify-between transition-all hover:-translate-y-1 hover:shadow-float"><BookOpenCheck className="h-7 w-7 text-primary" /><div><h2 className="font-display text-3xl font-bold">{exam.name}</h2><p className="mt-2 text-muted-foreground">{exam.fullName}</p><p className="mt-7 flex items-center gap-2 text-sm font-semibold text-primary">Explore materials <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1" /></p></div></Surface></Link> : <button key={exam.id} type="button" onClick={() => window.alert("UTBK content is coming soon.")} className="text-left"><Surface className="flex min-h-60 flex-col justify-between opacity-70 transition-opacity hover:opacity-100"><Clock3 className="h-7 w-7 text-muted-foreground" /><div><div className="mb-3 inline-flex rounded-full bg-secondary px-3 py-1 text-xs font-semibold text-muted-foreground">Content coming soon</div><h2 className="font-display text-3xl font-bold">{exam.name}</h2><p className="mt-2 text-muted-foreground">{exam.fullName}</p></div></Surface></button>)}</div></Page>; }