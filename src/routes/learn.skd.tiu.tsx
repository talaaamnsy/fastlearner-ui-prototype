import { createFileRoute } from "@tanstack/react-router";
import { Page, PageTitle, LinkRow, ProgressBar } from "@/components/app-ui";
import { tiuMaterials } from "@/data/mock-data";
export const Route = createFileRoute("/learn/skd/tiu")({ head: () => ({ meta: [{ title: "TIU Materials — FastLearner" }, { name: "description", content: "Choose one of ten TIU materials to study or practice." }, { property: "og:title", content: "TIU Materials — FastLearner" }, { property: "og:description", content: "Choose one of ten TIU materials to study or practice." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: TiuPage });
function TiuPage() { return <Page narrow><PageTitle title="TIU" subtitle="Choose a material to practice." back="/learn/skd" /><div className="space-y-3">{tiuMaterials.map((material) => <LinkRow key={material.id} to={`/material/${material.id}`} title={material.name} subtitle={`${material.questions} questions`} trailing={<div className="hidden w-28 sm:block"><div className="mb-1 text-right text-xs font-semibold text-muted-foreground">{material.mastery}%</div><ProgressBar value={material.mastery} /></div>} />)}</div></Page>; }