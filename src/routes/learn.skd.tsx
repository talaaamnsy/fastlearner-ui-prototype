import { createFileRoute, Link } from "@tanstack/react-router";
import { ChevronRight } from "lucide-react";
import { Page, PageTitle, ProgressBar, Surface } from "@/components/app-ui";
import { subtests } from "@/data/mock-data";

export const Route = createFileRoute("/learn/skd")({ head: () => ({ meta: [{ title: "SKD — FastLearner" }, { name: "description", content: "Study TWK, TIU, and TKP materials." }, { property: "og:title", content: "SKD — FastLearner" }, { property: "og:description", content: "Study TWK, TIU, and TKP materials." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: SkdPage });
function SkdPage() { return <Page><PageTitle title="SKD" subtitle="Build mastery across every subtest." back="/learn" /><div className="grid gap-5 md:grid-cols-3">{subtests.map((item) => <Link key={item.id} to={item.id === "tiu" ? "/learn/skd/tiu" : "/learn/skd"} onClick={item.id === "tiu" ? undefined : (event) => { event.preventDefault(); window.alert(`${item.name} overview is represented in this prototype; open TIU for the complete material flow.`); }}><Surface className="group transition-all hover:-translate-y-1 hover:shadow-float"><div className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">{item.fullName}</p><h2 className="mt-2 font-display text-3xl font-bold">{item.name}</h2></div><ChevronRight className="h-5 w-5 text-muted-foreground transition-transform group-hover:translate-x-1" /></div><div className="mt-10"><div className="mb-2 flex justify-between text-sm"><span>{item.count} Materials</span><strong>{item.mastery}%</strong></div><ProgressBar value={item.mastery} /></div></Surface></Link>)}</div></Page>; }